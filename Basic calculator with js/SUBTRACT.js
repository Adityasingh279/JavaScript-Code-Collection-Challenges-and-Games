const subtractArrow = (a, b) => {
  return a - b;
};

function subtract(a, b) {
  return a - b;
}

// Call the functions and log the results
console.log(subtractArrow(3, 8));
console.log(subtract(7, 9));
